const About = () => {
	return (
		<>
			<p className="fs-[1.3rem] c-[#B6B7B7] lh-[1.8]">
				<abbr title="Vite + React">Viact</abbr> is a starter template for React TypeScript that uses Vitejs, which supports RTL for
				specific styles, proxy fetching API (to fix CORS errors) and a simple mock REST API server. Additionally, Is set up VScode,
				Storybook, ErrorBoundary, UnoCss (atomic CSS engine), Eslint, Prettier, and more.
			</p>
		</>
	);
};

export default About;
